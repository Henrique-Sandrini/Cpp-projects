#include <iostream>
#include <cmath>
int main (){

    double x = 4.987;
    double y = 8;
    double z;

    //z = std::max(x,y); maior entre duas variaveis
    //z = std::min(x,y); menor entre duas variaveis

    //z = pow(9, 8); potencia (include cmath)

    //z = sqrt(64); raiz quadrada (include cmath)

    //z = abs(-8); valor absoulto (include cmath)
 
    //z = round(x); arredonda o valor (include cmath)

    //z = ceil (x); arredonda o valor pra cima (include cmath)

    //z = floor(x); arredonda " " "  pra baixo (include cmath)

    std::cout << z;
    return 0;
}